<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ghw.minibox.mapper.MbReplyMapper">

    <resultMap type="com.ghw.minibox.entity.MbReply" id="MbReplyMap">
        <result property="id" column="id" jdbcType="OTHER"/>
        <result property="type" column="type" jdbcType="VARCHAR"/>
        <result property="replyWho" column="reply_who" jdbcType="OTHER"/>
        <result property="replyContent" column="reply_content" jdbcType="VARCHAR"/>
        <result property="replyDate" column="reply_date" jdbcType="TIMESTAMP"/>
        <result property="replyUid" column="reply_uid" jdbcType="OTHER"/>
        <result property="replyInPost" column="reply_in_post" jdbcType="OTHER"/>
        <result property="replyInComment" column="reply_in_comment" jdbcType="OTHER"/>
        <result property="replyInGame" column="reply_in_game" jdbcType="OTHER"/>
        <result property="status" column="status" jdbcType="OTHER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MbReplyMap">
        select id,
               type,
               reply_who,
               reply_content,
               reply_date,
               reply_uid,
               reply_in_post,
               reply_in_comment,
               reply_in_game,
               status
        from minibox.mb_reply
        where id = #{id}
    </select>

    <select id="queryInId" resultMap="MbReplyMap">
        SELECT r.id, type, reply_who, reply_content, reply_date, reply_uid, reply_in_post, reply_in_comment,
        reply_in_game, status
        FROM mb_reply AS r
        WHERE r.reply_in_post IN
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MbReplyMap">
        select id,
               type,
               reply_who,
               reply_content,
               reply_date,
               reply_uid,
               reply_in_post,
               reply_in_comment,
               reply_in_game,
               status
        from minibox.mb_reply
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MbReplyMap">
        select
        id, type, reply_who, reply_content, reply_date, reply_uid, reply_in_post, reply_in_comment, reply_in_game,
        status
        from minibox.mb_reply
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="type != null and type != ''">
                and type = #{type}
            </if>
            <if test="replyWho != null">
                and reply_who = #{replyWho}
            </if>
            <if test="replyContent != null and replyContent != ''">
                and reply_content = #{replyContent}
            </if>
            <if test="replyDate != null">
                and reply_date = #{replyDate}
            </if>
            <if test="replyUid != null">
                and reply_uid = #{replyUid}
            </if>
            <if test="replyInPost != null">
                and reply_in_post = #{replyInPost}
            </if>
            <if test="replyInComment != null">
                and reply_in_comment = #{replyInComment}
            </if>
            <if test="replyInGame != null">
                and reply_in_game = #{replyInGame}
            </if>
            <if test="status != null">
                and `status` = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into minibox.mb_reply(type, reply_who, reply_content, reply_uid, reply_in_post,
                                     reply_in_comment, reply_in_game)
        values (#{type}, #{replyWho}, #{replyContent}, #{replyUid}, #{replyInPost}, #{replyInComment}, #{replyInGame})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into minibox.mb_reply(type, reply_who, reply_content, reply_date, reply_uid, reply_in_post,
        reply_in_comment, reply_in_game, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.type}, #{entity.replyWho}, #{entity.replyContent}, #{entity.replyDate}, #{entity.replyUid},
            #{entity.replyInPost}, #{entity.replyInComment}, #{entity.replyInGame}, #{entity.status})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into minibox.mb_reply(type, reply_who, reply_content, reply_date, reply_uid, reply_in_post,
        reply_in_comment, reply_in_game, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.type}, #{entity.replyWho}, #{entity.replyContent}, #{entity.replyDate}, #{entity.replyUid},
            #{entity.replyInPost}, #{entity.replyInComment}, #{entity.replyInGame}, #{entity.status})
        </foreach>
        on duplicate key update
        type = values(type) , reply_who = values(reply_who) , reply_content = values(reply_content) , reply_date =
        values(reply_date) , reply_uid = values(reply_uid) , reply_in_post = values(reply_in_post) , reply_in_comment =
        values(reply_in_comment) , reply_in_game = values(reply_in_game) , status = values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update minibox.mb_reply
        <set>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
            <if test="replyWho != null">
                reply_who = #{replyWho},
            </if>
            <if test="replyContent != null and replyContent != ''">
                reply_content = #{replyContent},
            </if>
            <if test="replyDate != null">
                reply_date = #{replyDate},
            </if>
            <if test="replyUid != null">
                reply_uid = #{replyUid},
            </if>
            <if test="replyInPost != null">
                reply_in_post = #{replyInPost},
            </if>
            <if test="replyInComment != null">
                reply_in_comment = #{replyInComment},
            </if>
            <if test="replyInGame != null">
                reply_in_game = #{replyInGame},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from minibox.mb_reply
        where id = #{id}
    </delete>

</mapper>