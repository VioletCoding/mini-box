<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ghw.minibox.mapper.MbGameMapper">

    <resultMap type="com.ghw.minibox.entity.MbGame" id="MbGameMap">
        <result property="gid" column="gid" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="price" column="price" jdbcType="NUMERIC"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
        <result property="gameState" column="game_state" jdbcType="INTEGER"/>
        <result property="score" column="score" jdbcType="NUMERIC"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="releaseTime" column="release_time" jdbcType="TIMESTAMP"/>
        <result property="developer" column="developer" jdbcType="VARCHAR"/>
        <result property="publisher" column="publisher" jdbcType="VARCHAR"/>
        <result property="scoreCount" column="score_count" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MbGameMap">
        select gid,
               name,
               price,
               state,
               game_state,
               score,
               description,
               release_time,
               developer,
               publisher,
               score_count
        from mini_box.mb_game
        where gid = #{gid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MbGameMap">
        select gid,
               name,
               price,
               state,
               game_state,
               score,
               description,
               release_time,
               developer,
               publisher,
               score_count
        from mini_box.mb_game
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MbGameMap">
        select
        gid, name, price, state, game_state, score, description, release_time, developer, publisher, score_count
        from mini_box.mb_game
        <where>
            <if test="gid != null">
                and gid = #{gid}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="gameState != null">
                and game_state = #{gameState}
            </if>
            <if test="score != null">
                and score = #{score}
            </if>
            <if test="description != null and description != ''">
                and description = #{description}
            </if>
            <if test="releaseTime != null">
                and release_time = #{releaseTime}
            </if>
            <if test="developer != null and developer != ''">
                and developer = #{developer}
            </if>
            <if test="publisher != null and publisher != ''">
                and publisher = #{publisher}
            </if>
            <if test="scoreCount != null">
                and score_count = #{scoreCount}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="gid" useGeneratedKeys="true">
        insert into mini_box.mb_game(name, price, state, game_state, score, description, release_time, developer,
                                     publisher, score_count)
        values (#{name}, #{price}, #{state}, #{gameState}, #{score}, #{description}, #{releaseTime}, #{developer},
                #{publisher}, #{scoreCount})
    </insert>

    <insert id="insertBatch" keyProperty="gid" useGeneratedKeys="true">
        insert into mini_box.mb_game(name, price, state, game_state, score, description, release_time, developer,
        publisher, score_count)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.price}, #{entity.state}, #{entity.gameState}, #{entity.score},
            #{entity.description}, #{entity.releaseTime}, #{entity.developer}, #{entity.publisher},
            #{entity.scoreCount})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="gid" useGeneratedKeys="true">
        insert into mini_box.mb_game(name, price, state, game_state, score, description, release_time, developer,
        publisher, score_count)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.price}, #{entity.state}, #{entity.gameState}, #{entity.score},
            #{entity.description}, #{entity.releaseTime}, #{entity.developer}, #{entity.publisher},
            #{entity.scoreCount})
        </foreach>
        on duplicate key update
        name = values(name) , price = values(price) , state = values(state) , game_state = values(game_state) , score =
        values(score) , description = values(description) , release_time = values(release_time) , developer =
        values(developer) , publisher = values(publisher) , score_count = values(score_count)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update mini_box.mb_game
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="gameState != null">
                game_state = #{gameState},
            </if>
            <if test="score != null">
                score = #{score},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="releaseTime != null">
                release_time = #{releaseTime},
            </if>
            <if test="developer != null and developer != ''">
                developer = #{developer},
            </if>
            <if test="publisher != null and publisher != ''">
                publisher = #{publisher},
            </if>
            <if test="scoreCount != null">
                score_count = #{scoreCount},
            </if>
        </set>
        where gid = #{gid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from mini_box.mb_game
        where gid = #{gid}
    </delete>

</mapper>