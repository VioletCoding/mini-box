spring:
  cache:
    type: redis
  redis:
    host: 127.0.0.1
    port: 6379
    lettuce:
      pool:
        #最大连接数
        max-active: 8
        #最大阻塞等待时间
        max-wait: -1ms
        #最大空闲连接
        max-idle: 8
        #最小空闲连接
        min-idle: 0
      shutdown-timeout: 3000ms