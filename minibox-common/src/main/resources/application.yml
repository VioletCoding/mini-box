spring:
  redis:
    host: localhost
    port: 6379
    jedis:
      pool:
        #最大连接数
        max-active: 8
        #最大阻塞等待时间
        max-wait: -1ms
        #最大空闲连接
        max-idle: 8
        #最小空闲连接
        min-idle: 0
    lettuce:
      shutdown-timeout: 3000ms